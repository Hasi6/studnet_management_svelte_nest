type User {
	_id: String!
	username: String!
	email: String!
	image: String!
	chatIdList: [String]!
	onlineStatus: Boolean!
}

type Chat {
	_id: String!
	users: [User]
}


type Query {
	searchUser(searchKey: String): [User]
}

type Mutation {
	getChatList(userId: String!): String 
}